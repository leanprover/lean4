-- Each abstract_ns[i] module contains a definition ns[i].foo with an abstracted nested proof.
-- This test makes sure that the abstracted proofs get different names.
import .abstract_ns1 .abstract_ns2

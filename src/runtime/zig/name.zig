// Name operations are implemented in `misc.zig` (`lean_name_eq`).
// This file is intentionally empty and kept for future name-related helpers.

const misc = @import("misc.zig");

test "name module compiles" {
    _ = misc;
}
